{
  "name": "@agentguard/agents",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "benchmark:diagnosis": "ts-node --transpile-only src/benchmark.ts",
    "benchmark:remediation": "ts-node --transpile-only src/remediationBenchmark.ts"
  },
  "dependencies": {
    "@agentguard/db": "*",
    "@aws-sdk/client-lambda": "^3.600.0",
    "@google/generative-ai": "^0.24.0",
    "@langchain/core": "^0.3.0",
    "@langchain/google-genai": "^0.1.0",
    "@langchain/langgraph": "^0.2.0",
    "dotenv": "^16.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
